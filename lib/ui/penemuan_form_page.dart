import 'package:flutter/material.dart';

class PenemuanFormPage extends StatelessWidget {
  const PenemuanFormPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Form Penemuan'),
      ),
      body: Center(
        child: Text('Penemuan Form Page'),
      ),
    );
  }
}
